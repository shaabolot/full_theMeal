import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const mealSlice = createSlice({
    name: 'meal',
    initialState,
    reducers: {

    }
})

export const {

} = mealSlice.actions;

export default mealSlice.reducer;